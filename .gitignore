# Rust
/target
**/*.rs.bk
Cargo.lock

# PHP / Composer
/vendor
composer.lock
.phpunit.cache
.phpunit.result.cache

# Editors / IDEs
.idea/
.vscode/
.zed/
*.code-workspace
.history/
*.swp
*.swo
*~

# OS junk
.DS_Store
._*
Thumbs.db
ehthumbs.db
Desktop.ini

# Local agent/dev scratch
/docs_local

# /wrush task worktrees — always created here, in-tree
/worktrees/

# Local-only cargo overrides (see switch_consumers_to_local_ktav.sh).
# Committing one makes the crate unbuildable everywhere else.
/.cargo/

# Node (scripts/build-docs.mjs)
/node_modules
